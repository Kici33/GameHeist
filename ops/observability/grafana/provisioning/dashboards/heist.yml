apiVersion: 1
providers:
  - name: GameHeist
    orgId: 1
    folder: GameHeist
    type: file
    disableDeletion: false
    editable: false
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
