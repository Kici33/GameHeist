# Copy to .env and replace all three values with independent random secrets.
# Disposable local development only; use separate scoped tokens in deployed environments.
INFLUX_PASSWORD=replace-with-random-password
INFLUX_TOKEN=replace-with-random-token
GRAFANA_PASSWORD=replace-with-random-password
