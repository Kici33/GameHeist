apiVersion: v1
kind: Service
metadata:
  name: gameheist
spec:
  # Private single-server lab access only, not a proxy/party routing layer.
  type: ClusterIP
  selector:
    app.kubernetes.io/name: gameheist
  ports:
    - name: minecraft
      port: 25565
      targetPort: minecraft
